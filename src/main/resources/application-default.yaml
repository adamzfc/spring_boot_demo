server:
  port: 8080

spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/demo?characterEncoding=utf-8
    username: root
    password: 123456

  thymeleaf:
    prefix: classpath:/public/thymeleaf/
    cache: false